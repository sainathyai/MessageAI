{
  "name": "messageai-push-notifications",
  "version": "1.0.0",
  "description": "AWS Lambda function for MessageAI push notifications via Expo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name MessageAI-PushNotifications --zip-file fileb://function.zip"
  },
  "keywords": [
    "push-notifications",
    "expo",
    "aws-lambda",
    "messageai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "expo-server-sdk": "^3.7.0"
  }
}

